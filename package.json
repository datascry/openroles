{
  "name": "openroles",
  "version": "0.0.0",
  "private": true,
  "description": "Static, queryable job board aggregator across major Applicant Tracking Systems",
  "license": "MIT",
  "author": "datascry",
  "type": "module",
  "engines": {
    "bun": ">=1.3.0"
  },
  "workspaces": [
    "shared",
    "scraper",
    "site"
  ],
  "scripts": {
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "format": "biome format --write .",
    "typecheck": "bun run --filter '*' typecheck",
    "test": "bun test --coverage",
    "test:watch": "bun test --watch",
    "scrape": "bun run --filter scraper scrape",
    "harvest": "bun run --filter scraper harvest",
    "build-db": "bun run --filter scraper build-db",
    "dev": "bun run --filter site dev",
    "build": "bun run --filter site build",
    "preview": "bun run --filter site preview",
    "e2e": "bun run --filter site e2e",
    "lighthouse": "bun run --filter site lighthouse",
    "size-limit": "bun run --filter site size-limit",
    "changelog": "git-cliff --output CHANGELOG.md"
  },
  "devDependencies": {
    "@biomejs/biome": "2.4.13",
    "@commitlint/cli": "20.5.2",
    "@commitlint/config-conventional": "20.5.0",
    "lefthook": "2.1.6",
    "typescript": "6.0.3"
  },
  "trustedDependencies": [
    "lefthook"
  ]
}
